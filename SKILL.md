---
name: narrated-video
description: 将对话文案或本地文本制作成图片运镜解说视频，也用于继续或局部修改已有 narrated-video 项目。适用于“把这段文字做成解说视频”“用这些图片配解说和字幕”“先做视频样片”等请求，支持可选风格、文案与 Demo 确认、MeloTTS 或已有配音、配乐和增量渲染。不用于仅润色文案、单独生成图片、普通视频剪辑或 AI 动态视频生成。
---

# 图片运镜解说视频

使用随附 `scripts/pipeline.py` 和项目 JSON 复用制作流程，不为每个主题重写脚本。图片与解说时间轴分离，目前仅支持 `type: image`；动态视频镜头需以后实现适配器。

## 先判断当前阶段

- **新项目**：读取输入 → 整理并确认文案 → 选择环境和风格 → 制作 Demo → 确认 Demo → 制作全片。
- **继续项目**：读取项目 JSON、`.narrated-video/state.json` 与最近验证报告，接着完成未完成的步骤。复用已有效的路径选择和批准，不重新走一遍首次制作流程。
- **局部修改**：先判断变化影响文案、Demo 还是其他镜头；修改配置/素材后运行相同入口。缓存会重做相关步骤及下游输出。

按需读取参考：

| 当前工作 | 参考 |
|---|---|
| 初始化、分句、镜头配置、渲染及验证 | [项目配置和命令](references/project.md) |
| 首次选择或切换软件/资源目录、启动失败 | [运行环境](references/runtime.md) |
| 选择或调整美术、口播与节奏 | [风格预设](references/styles.md) |

## 文案与制作确认

1. 接收对话文字、`.txt`、`.md` 或目录。对话文字保存为 UTF-8 `source.txt`；保留文件原文。目录只有一个候选时读取，多个时让用户选择。Markdown 标题、元数据和备注不要直接当作口播。
2. 根据完整性、口播适用性和目标时长润色、改写或补写；未给时长时提出建议。新增事实先查证，无法证实的内容明确说明。提交完整改写稿、主要调整及预计时长，等待用户批准，然后才生成配音和画面。
3. 将批准稿存为纯口播文本，按自然停顿分成带稳定 ID 的短句。合并后须与批准稿一致；不要为了字幕长度删改内容。用实际用户回复记录 `script` 批准。
4. 准备配乐前先询问创作者意图：情绪、时代/地域气质、乐器或类型、能量曲线、是否需要人声、目标平台和授权要求。根据回答检索至少 3 个候选，提供可试听预览链接、时长、来源和许可；创作者试听并明确选定后才下载。没有可用试听能力时只提供链接并暂停，不代替用户判断满意度。未获选择时使用 `music: []`，不得默默复用上一个项目或默认曲目。
5. 选择有代表性的连续约 20–40 秒制作 Demo，短片可更短，也不必选开头。先准备这部分的图像和配音，呈现所选风格、声音、字幕、运镜、转场与已批准的配乐；查看抽帧、试听并交给用户确认。
6. 用实际回复记录 Demo 批准后，再生成其余素材和配音、渲染全片。明确授权跳过某阶段时，在对应记录中保留用户原话和跳过范围。“开始制作”本身不表示跳过 Demo。

批准与路径选择都以实际对话为准；`record/configure` 只保存记录，不能代替用户做决定。为避免反复确认，将当前确实需要用户决定的问题集中提出，已有明确选择直接沿用。

文案或分句改变使 script 批准失效；配音、风格、字幕、输出规格、运行环境、音乐及 Demo 镜头变化使 Demo 批准失效。只替换非 Demo 镜头的素材或配音时，保留未受影响的批准。配置里改变音频路径不等于改变口播文字。

## 软件、素材和时间轴

- 首次制作前列出发现的软件/资源候选完整路径、用途和检查结果，让用户选择或提供路径；即使一个候选也不代选，除非用户已经明确选定。可一次选择整套环境。将选择保存到 `runtime`，后续同一项目直接复用；路径失效或要求切换时再选择。
- 使用已有 WAV 时只需 Python 和 FFmpeg；MeloTTS 需要包含它的 Python、NLTK 与模型缓存。具体字段及 `paths/configure/doctor` 用法见运行环境参考。找不到组件只说明当前搜索路径不可用，先核对解释器和已有资源目录，不直接判断本机未安装。
- 不自动安装依赖、字体或下载模型，不为排查路径关闭网络安全检查。新项目默认离线读已有模型，配置仅作用于任务进程；新增依赖/资源需相应授权。
- 优先用用户素材，缺图时使用可用的 imagegen 技能/工具，先生成 Demo 所需部分，并保持人物、美术和画幅一致。工具不可用时说明缺失项，不替换成无关素材。风格描述需落实为素材和具体参数，不声称仅填写 style 就会改变声音或图像。
- 配音按句生成或使用每句 PCM WAV，以真实样本时长对齐视频帧。整段录音先取得可靠分句边界再切分，不能用字数推算时间。每次渲染后检查 SRT 是否落在对应音频和镜头内、句间是否意外重叠或留空，并在首、中、尾及每个切镜点抽帧检查字幕安全区、对比度、字号和两行以内可读性；发现字幕过小或与切镜不匹配时先修正参数/分句再重渲染。
- 图片提示词、来源及音乐许可/署名记入素材清单。音乐候选试听记录、用户选择原话、下载地址、文件哈希和许可写入 `music-selection.json`；只有批准候选才进入 `audio/music/` 和项目 `music` 数组。支持多段音乐和淡入淡出；许可不明时不声称可商用。图片生成、联网查资料/找音乐和语音对齐由 agent 使用现有工具完成，脚本没有这些自动适配器。

## 验证和交付

运行脚本的媒体验证，并按需查看关键镜头、切换点和字幕，试听已批准音乐、配音与混音的开头、转场及结尾。失败保留缓存；同一错误重复两次先调查原因，不无限重试或删缓存掩盖问题。

交付 MP4、SRT、项目配置、素材清单、`music-selection.json` 和验证报告。说明实际观看/试听范围及未验证事项；完整解码通过不代表内容、声画同步或主观听感全部合格。移交可编辑工程时携带原项目和引用素材，单独 deliverables 目录不是可移植工程。
